function promoSlider() {
    $('.promo__slider').slick({
        speed: 1200,
        adaptiveHeight: true,
        arrows: false,
        fade: true,
        cssEasy: 'linear',
        //autoplay: true,
        //autoplaySpeed: 1200,
    });
}

export default promoSlider;